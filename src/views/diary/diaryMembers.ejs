<!-- 김서영_60221302_고급웹final -->
<link rel="stylesheet" href="/css/diaryMembers.css" />

<h2>가입 요청 목록</h2>

<% if (pendingMembers.length === 0) { %>
<p>대기 중인 가입 요청이 없습니다.</p>
<% } else { %>
<ul>
  <% pendingMembers.forEach(m => { %>
  <li>
    <span class="nick"><%= m.User.nick %></span>

    <div class="btn-group">
      <form
        action="/diary/<%= diaryId %>/members/<%= m.id %>/approve"
        method="POST"
      >
        <button type="submit" class="approve-btn">승인</button>
      </form>

      <form
        action="/diary/<%= diaryId %>/members/<%= m.id %>/reject"
        method="POST"
      >
        <button type="submit" class="reject-btn">거절</button>
      </form>
    </div>
  </li>

  <% }) %>
</ul>
<% } %>

<a href="/diary/<%= diaryId %>">← 다이어리로 돌아가기</a>
